<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mobileshop">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".Admin.ThemActivity"></activity>
        <activity android:name=".Admin.UpdateSanPhamActivity" />
        <activity android:name=".Admin.AdminQuanTriActivity" />
        <activity android:name=".Admin.AdminActivity" />
        <activity android:name=".Activiti.LienKetMangXaHoiActivity" />
        <activity android:name=".Activiti.DoiMatKhauActivity" />
        <activity android:name=".Activiti.LichSuMuaHangActivity" />
        <activity android:name=".Activiti.ThongTinTaikhoanActivity" />
        <activity android:name=".Activiti.LienHeActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".Activiti.ThongTinActivity"
            android:label="@string/title_activity_thong_tin" /> <!-- Include the AdActivity configChanges and theme. -->
        <meta-data
            android:name="com.google.android.gms.ads.AD_MANAGER_APP"
            android:value="true" />

        <activity android:name=".Activiti.ThongTinkhachhang" />
        <activity android:name=".Activiti.Giohang" />
        <activity android:name=".Activiti.LapTopActivity" />
        <activity android:name=".Activiti.ChiTietSanPham" />
        <activity android:name=".Activiti.DienThoaiActivity" />
        <activity android:name=".Activiti.RegisterActiviti" />
        <activity android:name=".Activiti.MainActivity" />
        <activity android:name=".Activiti.HomeActivity" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>